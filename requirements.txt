# neuralis + laap-AGI 可重現啟動依賴（Python 3.11–3.13）
# 作者 pyproject 的 psutil<7 與 hermes-agent 的 psutil==7.2.2 衝突，
# 這裡讓 psutil 跟隨 hermes-agent，從原始碼跑（不做 laap 的 editable install）。
hermes-agent
aiohttp>=3.9
pyyaml>=6
python-dotenv>=1
numpy>=1.24
